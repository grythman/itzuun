# KPI Regression Incident

## 1) Incident Header
- Incident ID: `INC-20260404-1`
- Date/Time (UTC): `2026-04-04T15:21:36Z`
- Detected by: `weekly_kpi_report` + `kpi-alerts-report`
- Owner: `@mike_ops`
- Severity: `sev2`

## 2) Trigger
- KPI affected: `completion_rate_pct`
- Current value: `15.0`
- Baseline (last 4 weeks avg): `>= 60.0` (threshold baseline for alerting)
- Delta (%): `-45.0` vs minimum expected threshold
- Threshold breached: `completion_rate_pct < 60.0`

## 3) Impact
- Affected cohort/project IDs: `pilot synthetic cohort` (root-cause isolation pending production segment breakdown)
- User impact summary: Completion performance appears below expected threshold; requires staged funnel diagnosis.
- Revenue/escrow impact: Potential downstream impact on release velocity and client trust if sustained.

## 4) Immediate Actions (0-4h)
1. Validate data correctness from KPI artifact + DB spot-check.
2. Identify top 3 causal segments (role, category, project stage).
3. Apply mitigation owner + ETA:
- Ops (`@mike_ops`): data validation and cohort integrity check.
- Product (`@copilot`): funnel-stage drop-off analysis.
- Support (`@julia_support`): dispute/completion case quality review.
4. Post status update in incident tracker.

## 5) Root Cause + Corrective Actions (24-72h)
- Root cause hypothesis: Synthetic bootstrap data includes incomplete completion distribution, causing low completion rate.
- Confirmed cause: Current 7-day cohort is heavily skewed to `in_progress` (`16/20`) with only `3/20` completed, so `completion_rate_pct=15.0` by construction.
- Corrective action(s):
  - Run production-only cohort segmentation query (separate from synthetic bootstrap cohort).
  - Split KPI reporting into `synthetic|production` cohort labels for alert evaluation.
  - Reconcile completion state transitions for production cohort projects in current KPI window.
- Owner per action: `@mike_ops`, `@copilot`, `@julia_support`
- Due date: `2026-04-07T12:00:00Z`

## 6) Exit Criteria
- KPI returns within threshold for 2 consecutive weekly runs.
- Evidence links attached (artifact, query, decision note).
- Postmortem status: `monitoring` (RC1 decision on 2026-04-06 UTC).

## 7) Evidence Bundle Format (Audit)
- `bundle_id`: `KPI-20260404-week14`
- `incident_id`: `INC-20260404-1`
- `artifact_link`: `https://github.com/grythman/itzuun/actions/runs/23979637084/artifacts/6270976944`
- `tracker_link`: `docs/PILOT_ROLLOUT_TRACKER.md`
- `query_or_log_link`: `docs/evidence/kpi_segment_report_20260404.json`
- `decision_note`: `Alert evaluator returned incident_decision=open due to completion_rate_pct below threshold. Segment report confirms 3 completed / 20 hired in 7-day window.`
- `approver`: `@copilot 2026-04-04T15:30:26Z`

## 8) Decision Notes (2026-04-07 UTC)
- RC1 incident close preparation reviewed.
- Priority auth verification did **not** pass yet in production (`client/freelancer login checks returned 400 Invalid credentials` during scripted check at `2026-04-07T04:21:01Z`).
- Incident close transition is deferred until:
  1) valid production auth verification evidence is attached, and
  2) KPI health criteria (2 consecutive weekly runs) are met.
